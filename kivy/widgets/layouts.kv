#:kivy 2.1.0

<MainScreen@BoxLayout>:
    orientation: "vertical"
    padding: 20
    spacing: 10

    # --- Top bar (battery + logo + home) ---
    BoxLayout:
        size_hint_y: 0.2
        spacing: 10

    # --- Middle section (2 stickmen) ---
    BoxLayout:
        size_hint_y: 0.8
        spacing: 40
        padding: 40